# Examples

This directory is reserved for non-sensitive sample inputs and outputs.

The repository does not include enterprise source documents. Use the commands in the root README with your own `.ceb` files, and keep generated output outside version control unless it is safe to publish.
